# User Prompt（用户级提示词）
id: image_translate.user
version: "1.0.0"
description: User prompt for translating OCR text into Simplified Chinese.
template: |
  请将以下文本翻译为简体中文：
  - 保留英文缩写、首字母缩写与专有名词（如 AI, CPU, OpenAI）。
  - 不修改占位符（如 <<TERM_1>>）。
  - 只输出翻译后的文本。

  文本：
  {{ content }}
